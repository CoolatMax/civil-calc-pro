# CLI Usage Guide: Civil Calc Pro

Civil Calc Pro includes a powerful Command Line Interface (CLI) for engineers who need to perform rapid calculations without launching a web browser. The CLI interacts directly with the `CivilCalc` logic engine.

---

## 🛠 Installation & Setup

Since the CLI is built with Node.js, ensure you have the environment set up in your terminal or GitHub Codespace.

1. **Navigate to the project root:**
   ```bash
   cd civil-calc-pro
   ```
2. **Ensure permissions: (Linux/Mac only)**
    ```
    chmod +x src/cli/calc.js
    ```
